import { StyleSheet } from 'react-native'

import { Fonts } from 'App/Themes'

export default StyleSheet.create({
  errorTrayBodyStyle: { paddingHorizontal: 16 },
  errorMessage: {
    fontSize: Fonts.size.regularSmall,
    paddingVertical: '6%'
  }
})
