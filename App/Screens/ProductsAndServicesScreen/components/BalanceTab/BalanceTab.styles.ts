import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  descriptionStyle: {
    width: 300,
    fontSize: 16.6,
    lineHeight: 22.9
  },
  rightIconContainer: {
    marginHorizontal: 8
  }
})

export default styles
